name: footy-predictor
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - streamlit
      - requests
      - beautifulsoup4
      - python-dotenv
      - llama-cpp-python
      - pandas
      - pytest
      - playwright
      - tiktoken
      - tavily-python